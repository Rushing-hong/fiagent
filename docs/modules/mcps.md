# mcps/

**路径**：`/mcps/`

## 作用

MCP（Model Context Protocol）配置占位，供未来接入外部 MCP 服务。

## 文件

| 文件 | 说明 |
|------|------|
| `mcp.json` | MCP 服务器声明 |
| `registry.py` | `MCPRegistry`，并入 `AgentContext` 扫描 |

当前以本地 tools 为主，MCP 为扩展预留。
